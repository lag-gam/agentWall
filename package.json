{
  "name": "agentfence",
  "private": true,
  "workspaces": ["frontend", "worker", "agent-server"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w worker\" \"npm run dev -w frontend\" \"npm run dev -w agent-server\"",
    "dev:no-agent": "concurrently \"npm run dev -w worker\" \"npm run dev -w frontend\"",
    "build": "npm run build -w worker && npm run build -w frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
